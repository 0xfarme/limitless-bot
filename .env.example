# ========== Network Configuration ==========
RPC_URL=https://mainnet.base.org
CHAIN_ID=8453

# ========== Market Configuration ==========
# Comma-separated list of oracle IDs to track (supports multiple markets)
# Use PRICE_ORACLE_IDS for multiple: PRICE_ORACLE_IDS=58,59,60
# Or PRICE_ORACLE_ID for single: PRICE_ORACLE_ID=58
PRICE_ORACLE_IDS=58,59
FREQUENCY=hourly

# ========== Wallet Configuration ==========
# Comma-separated list of private keys (no 0x prefix or with 0x both supported)
PRIVATE_KEYS=0xxxxxxxxx

# ========== Trading Strategy ==========
# Poll interval in milliseconds (how often to check market)
POLL_INTERVAL_MS=20000

# Position size in USDC
BUY_AMOUNT_USDC=5

# Profit target percentage
TARGET_PROFIT_PCT=20

# Slippage tolerance in basis points (100 = 1%)
SLIPPAGE_BPS=100

# ========== Stop Loss Configuration ==========
# Enable/disable stop loss protection
STOP_LOSS_ENABLED=true

# Activate stop loss in the last X minutes before market deadline
STOP_LOSS_TRIGGER_MINUTES=13

# Trigger stop loss at X% loss (use negative number, e.g., -30 for -30% loss)
STOP_LOSS_PCT=-30

# ========== Entry Strategy ==========
# STRATEGY_MODE options:
#   - "dominant": Buy the side with price >= TRIGGER_PCT
#   - "opposite": If a side is within TRIGGER_BAND of TRIGGER_PCT, buy the opposite side
STRATEGY_MODE=dominant

# Price threshold to trigger a buy (percentage)
TRIGGER_PCT=60

# Price band for opposite strategy (percentage)
TRIGGER_BAND=5

# ========== Gas Configuration ==========
# Maximum gas to spend per transaction in ETH
MAX_GAS_ETH=0.015

# Gas price in Gwei
GAS_PRICE_GWEI=0.005

# Number of confirmations to wait for
CONFIRMATIONS=1

# ========== Logging Configuration ==========
# Directory for log files (optional, defaults to data/logs)
# LOG_DIR=data/logs

# State file location (optional, defaults to data/state.json)
# STATE_FILE=data/state.json